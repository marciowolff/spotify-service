{
  "name": "spotify-api-client",
  "version": "4.0.0",
  "description": "Cliente HTTP para conexão com API's utilizada no projeto Spotify",
  "main": "build/index.js",
  "scripts": {
    "test": "jest",
    "build": "babel src --out-dir build --source-maps --ignore '**/*.test.js'",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "babel-core": "7.0.0-bridge.0",
    "babel-eslint": "^10.0.1",
    "babel-jest": "^23.6.0",
    "babel-preset-jest": "^23.2.0",
    "eslint": "^5.6.1",
    "eslint-plugin-jest": "^21.24.1",
    "husky": "^1.1.1",
    "jest": "^23.6.0"
  },
  "dependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/polyfill": "^7.0.0",
    "@babel/preset-env": "^7.1.0",
    "axios": "^0.18.0"
  }
}
